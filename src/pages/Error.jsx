import React from 'react';

const Error = () => {
    return (
        <div>
            <h1 style={{color: "darkred", display: "flex", justifyContent: "center"}}>
                Вы перешли на несуществующую страницу
            </h1>
        </div>
    );
};

export default Error;